{
  "name": "mploycheck",
  "version": "1.0.0",
  "description": "MployCheck Employee Verification Portal",
  "scripts": {
    "build": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}
